import { StyleSheet } from "react-native"

const estilo = StyleSheet.create({
    naMesmaLinha: {
        flexDirection: "row",
        alignItems: "center"
    },
    imgSend:{
        width:30,
        height:30,
        margin:10
    }
})

export default estilo;